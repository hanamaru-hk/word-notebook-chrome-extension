# config

This library is used to handle the configuration which is stored in chrome.storage.local.

## structure

```json
{
    "config": {
        "from": "JP",
        "to": "zh-TW",
        "llm": "perplexity",
        "language": "en"
    }
}
```